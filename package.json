{
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && rm -rf dist && cp -R frontend/dist dist",
    "vercel-build": "npm --prefix backend ci && npm --prefix backend run vercel-build"
  },
  "dependencies": {
    "docker": "^1.0.0"
  }
}
